> Read detailed change logs in https://github.com/sakurawald/fuji/commits/dev/

- feature: new module `command_meta.when_online`, to provide the `/when-online <player> <cmd>` command. (Execute the command exactly once when the target player online.)
- [core] feature: now will always `log the console` when failed to execute commands in `command executor`. (Make it easier to debug.)
