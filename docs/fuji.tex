% Preamble
\documentclass[10pt,oneside]{book}

% Packages

% use a4 paper
\usepackage[a4paper, total={6in, 8in}]{geometry}

% color extension
\usepackage{xcolor}

% colored box
\usepackage{tcolorbox}

% hyper link colorize
\usepackage{hyperref}
\hypersetup{
    colorlinks=true,
    linkcolor=blue,
    urlcolor=red
}

% the 'outputdir' is reletive to fuji.tex, and must point the 'output directory' of latex compiler.
\usepackage{minted}

% fix the character '_' can't be copied in output .pdf file
\usepackage[T1]{fontenc}

% disable double hyphen ligature in typeset
\usepackage{microtype}
\DisableLigatures[-]{}

%\usepackage[parfill]{parskip}

% to listing a kind of objects
\usepackage{listings}

% Macros
\input{macros/macro}

% use fancy header and footer
\usepackage{fancyhdr}
\pagestyle{fancy}
%\fancyhf{}
%\fancyfoot[LE,RO]{\thepage}
%\fancyhead[RO]{\textbf{\nouppercase{\rightmark}}}
%\fancyhead[LE]{\slshape\nouppercase{\leftmark}}
\fancyhead[LE,RO]{\nouppercase{\rightmark}}
\fancyhead[LO,RE]{\textbf{\nouppercase{\leftmark}}}
\fancyfoot[C]{\thepage}

% Document
\begin{document}

    \title{
        \textbf{Fuji Manual}\\[0.5em]
        \large {\version}
    }
    \author{sakurawald}
    \date{\today}
    \maketitle

    % make toc
    \tableofcontents
    \mainmatter

    % input chapters
    \input{chapters/concept/concept}
    \input{chapters/command/command}
    \input{chapters/permission/permission}
    \input{chapters/meta/meta}
    \input{chapters/configuration/configuration}
    \input{chapters/language/language}
    \input{chapters/qna/qna}
    \input{chapters/suggestion/suggestion}

\end{document}
