package mod.fuji.evaluator.evaluator.vm;

/**
 * A Lisp machine gives your better control of the control, which is the weakness of the JVM.
 * It's hard to map the semantics from Lisp symbols to JVM bytecodes, especially for some Lisp symbols that have a rich and subtle semantics.
 */
public class LispMachine {


}
